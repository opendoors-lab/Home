# Core
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=mobimatesHome
FRONTEND_URL=http://localhost:3000
NODE_ENV=development
PORT=9000
API_PUBLIC_URL=http://localhost:9000

# Owner & auth
OWNER_EMAIL=opendoors.ethiopia@gmail.com
OWNER_PASSWORD=ChangeMe123!
SUPER_ADMIN_EMAIL_2=superadmin2@yourdomain.com
SUPER_ADMIN_PASSWORD_2=ChangeMe123!
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=90d
# Long-lived refresh for persistent login (optional)
REFRESH_TOKEN_TTL=3650d
PASSWORD_RESET_TTL_MIN=60
REVALIDATE_SECRET=change-me-revalidate-secret

# Mailer (GoDaddy / Titan — use your mailbox password, not an app password)
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=support@yourdomain.com
SMTP_PASS=yNBZ.VZc@WHqgm3
SMTP_AUTH_METHOD=LOGIN
SMTP_CONNECTION_TIMEOUT_MS=60000
SMTP_GREETING_TIMEOUT_MS=60000
SMTP_SOCKET_TIMEOUT_MS=60000
MAIL_FROM="MobiMates Support <support@yourdomain.com>"

# Uploads (local disk)
UPLOAD_DIR=upload

# Frontend (mobimates/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:9000
